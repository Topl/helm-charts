apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: {{ include "vertx.fullname" . }}
spec:
  healthCheck:
    checkIntervalSec: 60
    port: {{ .Values.service.port }}
    type: HTTPS
    requestPath: /healthz