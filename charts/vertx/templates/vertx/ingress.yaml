apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vertx-ingress
spec:
  tls:
  - secretName: vertx-tls
  rules:
  - http:
      paths:
      - path: /*
        pathType: ImplementationSpecific
        backend:
          service:
            name: vertx-svc-dev
            port:
              number: 60000